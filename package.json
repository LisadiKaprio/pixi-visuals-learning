{
  "name": "pixi-first-test",
  "version": "1.0.0",
  "description": "pixi test first by lisa",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w"
  },
  "dependencies": {
    "@types/pixi.js": "^5.0.0",
    "browser-sync": "^3.0.2",
    "npm-run-all": "^4.1.5",
    "pixi.js": "^7.3.3",
    "typescript": "^5.3.3"
  }
}
